# personal: names, DNS profile IDs, network details, credentials
local/
*.local
user_credentials.json

# build artefacts
release/
*.iso

# Optional family-recorded KLettres sets can be large and remain local until reviewed.
# The production launcher WAV labels under assets/voice are intentionally versioned.
assets/klettres/**/*.ogg

.DS_Store
__pycache__/
